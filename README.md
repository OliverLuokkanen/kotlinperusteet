# Kotlin perusteet – Todo

Jetpack Compose -sovellus, jossa näytetään tehtävälista (mock-data) ja käsitellään listaa Kotlin-funktioilla.

## Sisältö
- `Task` (data class) + `mockTasks`
- Funktiot: `addTask`, `toggleDone`, `filterByDone`, `sortByDueDate`, `deleteTask`, `updateTask`
- UI: listan näyttö + napit (Add/Toggle/Filter/Sort/Delete/Edit)

## APK (debug)
Android Studio: **Build → Build APK(s)**  
Tiedosto: `app/build/outputs/apk/debug/app-debug.apk`
